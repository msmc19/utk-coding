//this is super important too, it's the scanner
import java.util.Scanner;

class notes1 {
    public static void main (String[] args){
        /*check this shit out, this is a comment*/
        //these are integar variables

        int a = 10;
        double b = 25;
        System.out.println(a+b);

        //this is a string variable, u have to 
        // capitalize this one for some reason...
        String c = "sometingz";
        System.out.println(c);

        //this is a boolean, these will be important later
        //Bools can be true or false, but thats all.
        Boolean thanks;
        if (mom.equals gay){
            thanks = true;
        }

        //the scanner is how we get data/information from 
        //the console through inputs
        Scanner s = new Scanner(system.in);
        //this is how we declare the use of the scanner, it's done this way every time
        something = s.nextline();

    }
}
